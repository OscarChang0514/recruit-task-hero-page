import { StyledHeroProfile } from './HeroProfile.style';
import type { HeroProfileProps } from './HeroProfile.type';

const HeroProfile: React.FC<HeroProfileProps> = (props) => {
  return <StyledHeroProfile />;
};

export default HeroProfile;
